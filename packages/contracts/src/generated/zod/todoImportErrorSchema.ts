/**
* Generated by Kubb (https://kubb.dev/).
* Do not edit manually.
*/

import type { TodoImportError } from "../types/TodoImportError.ts";
import type { ToZod } from "@kubb/plugin-zod/utils";
import { z } from "zod";

/**
 * @description One contract violation. `row` is the 1-based data row (0 = the file itself).
 */
export const todoImportErrorSchema = z.object({
    "row": z.number().int().min(0),
"field": z.string(),
"message": z.string()
    }).describe("One contract violation. `row` is the 1-based data row (0 = the file itself).") as unknown as ToZod<TodoImportError>

export type TodoImportErrorSchema = TodoImportError