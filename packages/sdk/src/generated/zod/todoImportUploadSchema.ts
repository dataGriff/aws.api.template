/**
* Generated by Kubb (https://kubb.dev/).
* Do not edit manually.
*/

import type { TodoImportUpload } from "../types/TodoImportUpload.ts";
import type { ToZod } from "@kubb/plugin-zod/utils";
import { z } from "zod";

/**
 * @description Pre-signed S3 `POST` target. Send `multipart/form-data` to `url` with every entry of `fields`\nas a form field (verbatim, order preserved), then the CSV as the last part named `file`.\nFiles larger than `max_bytes` or with another content type are refused by S3 itself.\n
 */
export const todoImportUploadSchema = z.object({
    "url": z.string().url(),
"fields": z.object({
    
    }).catchall(z.string()),
"expires_at": z.string().datetime(),
"max_bytes": z.number().int().min(1)
    }).describe("Pre-signed S3 `POST` target. Send `multipart/form-data` to `url` with every entry of `fields`\nas a form field (verbatim, order preserved), then the CSV as the last part named `file`.\nFiles larger than `max_bytes` or with another content type are refused by S3 itself.\n") as unknown as ToZod<TodoImportUpload>