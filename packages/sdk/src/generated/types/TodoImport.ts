/**
* Generated by Kubb (https://kubb.dev/).
* Do not edit manually.
*/

import type { TodoImportError } from "./TodoImportError.ts";
import type { TodoImportStatus } from "./TodoImportStatus.ts";
import type { TodoImportUpload } from "./TodoImportUpload.ts";

export type TodoImport = {
    /**
     * @type string, uuid
    */
    import_id: string;
    /**
     * @description `awaiting_upload` until the file arrives, `processing` while it is validated, then\n`completed` (every row created) or `rejected` (nothing created, file quarantined).\n
     * @type string
    */
    status: TodoImportStatus;
    /**
     * @type string
    */
    file_name: string;
    /**
     * @description Pre-signed S3 `POST` target. Send `multipart/form-data` to `url` with every entry of `fields`\nas a form field (verbatim, order preserved), then the CSV as the last part named `file`.\nFiles larger than `max_bytes` or with another content type are refused by S3 itself.\n
     * @type object | undefined
    */
    upload?: TodoImportUpload;
    /**
     * @description Data rows found in the file (null until processed)
     * @minLength 0
     * @type integer
    */
    row_count?: number | null;
    /**
     * @description Todos created (equals row_count when completed, 0 when rejected)
     * @minLength 0
     * @type integer
    */
    created_count?: number | null;
    /**
     * @description First contract violations when rejected (the full report is quarantined with the file)
     * @type array | undefined
    */
    errors?: TodoImportError[];
    /**
     * @type string, date-time
    */
    created_at: string;
    /**
     * @type string, date-time
    */
    updated_at: string;
    /**
     * @type string, date-time
    */
    completed_at?: string | null;
};