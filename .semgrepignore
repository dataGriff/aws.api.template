# Paths Semgrep (task security:code) does not scan. Keep the reasons.
# Default ignores (node_modules, .git, dist, ...) still apply.
:include .gitignore
# Rendered from the contract by `task gen` (AWS gateway extensions only).
infra/terraform/modules/api-gateway/openapi.gateway.yaml
